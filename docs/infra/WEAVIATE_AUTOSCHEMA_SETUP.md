# Weaviate AutoSchema для Симбиосети

## Обзор

Для симбиосети используется **встроенная AutoSchema Weaviate**, которая позволяет схеме эволюционировать органически по мере обнаружения новых симбиотических связей и паттернов.

## Настройка AutoSchema

### Два уровня настройки

AutoSchema настраивается на двух уровнях:

1. **Уровень Weaviate сервера** - включает/выключает AutoSchema в самом Weaviate
2. **Уровень приложения** - говорит приложению использовать встроенную AutoSchema вместо ручного управления схемой

### 1. Настройка Weaviate сервера

```bash
# В docker-compose.yml или переменных Dokku для Weaviate
environment:
  AUTOSCHEMA_ENABLED: 'true'
  AUTOSCHEMA_DEFAULT_NUMBER: 'number'
  AUTOSCHEMA_DEFAULT_DATE: 'date'
```

### 2. Настройка приложения Симбиосети

```bash
# В переменных окружения приложения
WEAVIATE_USE_BUILTIN_AUTOSCHEMA=true  # Использовать встроенную AutoSchema (по умолчанию: true)
WEAVIATE_AUTOSCHEMA_DEFAULT_NUMBER=number  # Тип для чисел (опционально)
WEAVIATE_AUTOSCHEMA_DEFAULT_DATE=date  # Тип для дат (опционально)
```

### Параметры AutoSchema

#### Параметры Weaviate сервера

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `AUTOSCHEMA_ENABLED` | `'true'` | Включить автоматическое создание схемы в Weaviate |
| `AUTOSCHEMA_DEFAULT_NUMBER` | `'number'` | Тип для числовых значений (number vs int) |
| `AUTOSCHEMA_DEFAULT_DATE` | `'date'` | Тип для датоподобных значений |

#### Параметры приложения

| Переменная | Значение по умолчанию | Описание |
|-----------|----------------------|----------|
| `WEAVIATE_USE_BUILTIN_AUTOSCHEMA` | `true` | Использовать встроенную AutoSchema Weaviate вместо ручного управления схемой |
| `WEAVIATE_AUTOSCHEMA_DEFAULT_NUMBER` | `number` | Тип для числовых значений (передается в Weaviate) |
| `WEAVIATE_AUTOSCHEMA_DEFAULT_DATE` | `date` | Тип для датоподобных значений (передается в Weaviate) |

## Как работают два уровня настройки вместе

### Сценарий 1: Полное использование AutoSchema (рекомендуется)

```bash
# 1. Включить AutoSchema в Weaviate сервере
dokku config:set weaviate AUTOSCHEMA_ENABLED=true

# 2. Включить использование AutoSchema в приложении
dokku config:set simbioset-website WEAVIATE_USE_BUILTIN_AUTOSCHEMA=true
```

**Результат:**
- Приложение не создает схему вручную через Python код
- Weaviate автоматически создает и адаптирует схему при добавлении данных
- Идеально для симбиосети с динамической структурой данных

### Сценарий 2: Ручное управление схемой

```bash
# 1. Отключить AutoSchema в Weaviate (опционально, но рекомендуется)
dokku config:set weaviate AUTOSCHEMA_ENABLED=false

# 2. Отключить использование AutoSchema в приложении
dokku config:set simbioset-website WEAVIATE_USE_BUILTIN_AUTOSCHEMA=false
```

**Результат:**
- Приложение создает схему вручную через `api/storage/weaviate_schema.py`
- Полный контроль над структурой данных
- Подходит для production с фиксированной схемой

### Важно

- `AUTOSCHEMA_ENABLED` (Weaviate) должен быть `true`, если используется `WEAVIATE_USE_BUILTIN_AUTOSCHEMA=true` (приложение)
- Если `WEAVIATE_USE_BUILTIN_AUTOSCHEMA=false`, приложение будет пытаться создать схему вручную, даже если `AUTOSCHEMA_ENABLED=true` в Weaviate

## Как работает AutoSchema в Симбиосети

### 1. Автоматическое создание коллекций
```python
# При добавлении первого объекта в несуществующую коллекцию
client.data_object.create({
    "class": "Symbiosis",
    "properties": {
        "organism1": "дуб",
        "organism2": "микориза",
        "relation_type": "mutualism",
        "evidence": "улучшает поглощение фосфора"
    }
})
# → Автоматически создается коллекция "Symbiosis"
```

### 2. Добавление новых свойств
```python
# Добавление объекта с новым свойством
client.data_object.create({
    "class": "Symbiosis",
    "properties": {
        "organism1": "дуб",
        "organism2": "микориза",
        "relation_type": "mutualism",
        "evidence": "улучшает поглощение фосфора",
        "confidence_score": 0.95,  # Новое свойство
        "discovery_date": "2024-01-01"  # Новое свойство
    }
})
# → Автоматически добавляются свойства confidence_score и discovery_date
```

### 3. Инференс типов данных
- `strings` → `text`
- `numbers` → `number` (или `int` если целые)
- `booleans` → `boolean`
- `dates` → `date`
- `arrays` → `text[]`, `number[]`, `boolean[]`, `date[]`
- `objects` → `object[]`

## Преимущества для Симбиосети

### 🌱 **Органическая эволюция**
- Новые типы симбиотических связей добавляются автоматически
- Структура данных растет вместе с пониманием экосистем
- Нет необходимости в миграциях схемы

### 🔬 **Исследовательский подход**
- Можно экспериментировать с новыми типами данных
- Автоматическое обнаружение паттернов
- Гибкость в анализе симбиотических сетей

### 📈 **Масштабируемость**
- Схема адаптируется к объему данных
- Поддержка сложных вложенных структур
- Эффективное хранение мета-данных

## Производственное использование

### Включение в Dokku
```bash
# Настройка переменных окружения
dokku config:set weaviate AUTOSCHEMA_ENABLED=true
dokku config:set weaviate AUTOSCHEMA_DEFAULT_NUMBER=number
dokku config:set weaviate AUTOSCHEMA_DEFAULT_DATE=date
```

### Мониторинг схемы
```bash
# Проверка текущей схемы
curl http://weaviate:8080/v1/schema

# Проверка определенной коллекции
curl http://weaviate:8080/v1/schema/Symbiosis
```

## Ограничения и рекомендации

### ⚠️ Когда НЕ использовать AutoSchema
- **Строгие контракты API** - когда схема должна быть фиксированной
- **Высоконагруженные системы** - overhead на инференс типов
- **Командная разработка** - без контроля версии схемы

### ✅ Когда использовать AutoSchema
- **Исследовательские проекты** - как симбиосеть
- **Прототипирование** - быстрая итерация
- **Динамические данные** - часто меняющаяся структура

## Миграция с фиксированной схемы

### Переход на AutoSchema
1. Включить `AUTOSCHEMA_ENABLED=true`
2. Убедиться, что существующие данные совместимы
3. Начать добавлять новые данные с расширенной структурой
4. Постепенно расширять схему

### Резервное копирование
```bash
# Создать бэкап перед миграцией
docker exec weaviate weaviate backup create --name migration_backup
```

## Примеры использования в коде

### Python клиент
```python
import weaviate

client = weaviate.Client("http://localhost:8080")

# Добавление данных - схема создается автоматически
client.data_object.create({
    "class": "EcosystemAnalysis",
    "properties": {
        "ecosystem_name": "тайга",
        "biodiversity_index": 0.87,
        "symbiosis_count": 45,
        "dominant_species": ["сосна", "ель", "кедр"],
        "threats": ["пожары", "вырубка"],
        "conservation_status": "vulnerable",
        "last_updated": "2024-01-15T10:00:00Z"
    }
})
```

### Типы данных в AutoSchema
```python
# Примеры автоматического инференса типов
{
    "text_field": "текст" → DataType.TEXT,
    "number_field": 42.5 → DataType.NUMBER,
    "int_field": 42 → DataType.INT,
    "bool_field": True → DataType.BOOLEAN,
    "date_field": "2024-01-01" → DataType.DATE,
    "array_field": ["a", "b", "c"] → DataType.TEXT_ARRAY,
    "object_field": {"nested": "data"} → DataType.OBJECT
}
```

## Troubleshooting

### Схема не создается
```bash
# Проверить настройки
curl http://weaviate:8080/v1/meta | jq .modules

# Проверить логи
docker logs weaviate
```

### Конфликты типов
```
Error: property "field_name" already exists with different type
```
**Решение:** Удалить конфликтующее свойство или изменить тип данных.

### Производительность
- AutoSchema добавляет overhead на инференс типов
- Для высокой нагрузки рассмотреть отключение после стабилизации схемы

## Связанная документация

### Внутренняя документация
- [WEAVIATE_AUTOSCHEMA_ENTITY_CREATION.md](WEAVIATE_AUTOSCHEMA_ENTITY_CREATION.md) - Детальный анализ процесса создания сущностей и свойств
- [WEAVIATE_DEPLOY_AUTOSCHEMA.md](WEAVIATE_DEPLOY_AUTOSCHEMA.md) - Развертывание с AutoSchema в Dokku
- [WEAVIATE_SCHEMA_WORKFLOW.md](WEAVIATE_SCHEMA_WORKFLOW.md) - Workflow работы со схемой

### Официальная документация Weaviate
- [Weaviate AutoSchema](https://docs.weaviate.io/weaviate/config-refs/collections#auto-schema)
- [Schema Management](https://docs.weaviate.io/weaviate/config-refs/schema)
- [Collection Definition](https://docs.weaviate.io/developers/weaviate/config-refs/collections)