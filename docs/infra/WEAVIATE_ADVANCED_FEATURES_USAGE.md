# Использование продвинутых возможностей Weaviate

## Быстрый старт

Все продвинутые возможности включены по умолчанию через переменные окружения.

### Настройки (опционально)

```bash
# Включить Hybrid Search (по умолчанию: true)
WEAVIATE_USE_HYBRID_SEARCH=true

# Баланс между BM25 и векторным поиском (0-1, по умолчанию: 0.5)
WEAVIATE_HYBRID_ALPHA=0.5

# Включить Cross-Encoder Reranking (по умолчанию: false)
WEAVIATE_USE_RERANKING=false

# Количество кандидатов для reranking (по умолчанию: 50)
WEAVIATE_RERANK_LIMIT=50

# Размер кеша эмбеддингов (по умолчанию: 1000)
WEAVIATE_EMBEDDING_CACHE_SIZE=1000
```

## Примеры использования

### 1. Hybrid Search (автоматически включен)

```python
from api.storage.weaviate_storage import WeaviateStorage

storage = WeaviateStorage()

# Hybrid Search используется автоматически
results = storage.search_similar(
    query="дуб и микориза",
    document_id="session_123",
    top_k=10,
)

# Или явно указать параметры
results = storage.search_similar(
    query="дуб и микориза",
    document_id="session_123",
    top_k=10,
    use_hybrid=True,  # Включить Hybrid Search
    hybrid_alpha=0.5,  # Баланс: 0.5 = равный вес BM25 и векторного поиска
)
```

**Когда использовать разные значения alpha:**
- `alpha=0.3` - больше BM25 (для точных терминов: имена организмов, локации)
- `alpha=0.5` - баланс (рекомендуется для большинства случаев)
- `alpha=0.7` - больше векторного поиска (для концептуальных запросов)

### 2. Расширенная фильтрация

```python
# OR фильтры - любой из тегов
results = storage.search_similar(
    query="экосистема",
    document_id="session_123",
    top_k=10,
    tags_filter=["ecosystem_risk", "ecosystem_vulnerability"],  # Любой из тегов
)

# NOT фильтры - исключить теги
results = storage.search_similar(
    query="экосистема",
    document_id="session_123",
    top_k=10,
    exclude_tags=["deprecated", "outdated"],  # Исключить эти теги
)

# Диапазоны по timestamp
from datetime import datetime, timedelta

week_ago = int((datetime.now() - timedelta(days=7)).timestamp())
results = storage.search_similar(
    query="экосистема",
    document_id="session_123",
    top_k=10,
    timestamp_from=week_ago,  # Только за последнюю неделю
)
```

**Использование через _build_filters:**

```python
# Сложные комбинации фильтров
filter_obj = storage._build_filters(
    document_id="session_123",
    tags_filter=["ecosystem_risk", "ecosystem_vulnerability"],  # OR
    exclude_tags=["deprecated"],  # NOT
    timestamp_from=week_ago,
    location_filter="Москва",
)
```

### 3. Cross-Encoder Reranking

```python
# Включить reranking для критических запросов
results = await storage.search_similar_paragraphs(
    query="симбиоз между дубом и микоризой",
    document_id="session_123",
    top_k=10,
    use_reranking=True,  # Включить reranking
)

# Или через отдельный метод
results = storage.search_with_reranking(
    query="симбиоз между дубом и микоризой",
    document_id="session_123",
    top_k=10,
    rerank_limit=50,  # Получить 50 кандидатов, затем переранжировать до 10
)
```

**Когда использовать reranking:**
- Критически важные запросы (RAG для LLM)
- Короткие запросы пользователей
- Когда нужна максимальная точность

**Производительность:**
- Reranking добавляет ~50-100ms к времени поиска
- Используйте только для важных запросов

### 4. Кеширование эмбеддингов

Кеширование включено автоматически (размер: 1000 по умолчанию).

```python
# Кеш работает автоматически
storage = WeaviateStorage()

# Первый вызов - создает эмбеддинг
embedding1 = storage._create_embedding("дуб и микориза")

# Второй вызов - использует кеш (быстрее)
embedding2 = storage._create_embedding("дуб и микориза")  # Из кеша!

# Разные тексты - создает новый эмбеддинг
embedding3 = storage._create_embedding("ель и сосна")
```

**Настройка размера кеша:**

```bash
# Увеличить размер кеша для большего покрытия
WEAVIATE_EMBEDDING_CACHE_SIZE=5000

# Отключить кеширование
WEAVIATE_EMBEDDING_CACHE_SIZE=0
```

## Интеграция в существующий код

### Обновление context_builder.py

```python
# api/chat/context_builder.py

# Использовать Hybrid Search и reranking
similar_paragraphs = await storage.search_similar_paragraphs(
    query=message,
    document_id=session_id,
    top_k=5,
    use_reranking=True,  # Включить reranking для лучшей точности
)
```

### Обновление routes.py

```python
# api/storage/routes.py

# Использовать расширенную фильтрацию
results = storage.search_similar(
    query=search_request.query,
    document_id=search_request.document_id,
    top_k=search_request.top_k,
    tags_filter=search_request.tags,  # OR фильтр
    exclude_tags=search_request.exclude_tags,  # NOT фильтр
    timestamp_from=search_request.timestamp_from,  # Диапазон
    timestamp_to=search_request.timestamp_to,
)
```

## Производительность

### Ожидаемые улучшения

**Hybrid Search:**
- Точность: +20-30%
- Время: +10-20ms (незначительное увеличение)

**Cross-Encoder Reranking:**
- Точность: +10-15%
- Время: +50-100ms (для rerank_limit=50)

**Кеширование эмбеддингов:**
- Ускорение: 10-50x для повторяющихся запросов
- Память: ~1-5MB на 1000 эмбеддингов (зависит от размерности)

### Рекомендации

1. **Для большинства запросов:**
   - Hybrid Search включен (alpha=0.5)
   - Reranking выключен (для скорости)

2. **Для критических запросов (RAG для LLM):**
   - Hybrid Search включен
   - Reranking включен (для максимальной точности)

3. **Для точных терминов (имена организмов, локации):**
   - Hybrid Search с alpha=0.3 (больше BM25)

4. **Для концептуальных запросов:**
   - Hybrid Search с alpha=0.7 (больше векторного поиска)

## Отладка

### Логирование

Все методы логируют используемые возможности:

```
🔍 Hybrid Search (alpha=0.5): BM25 + векторный поиск
🎯 Reranking: 50 кандидатов → 10 результатов
💾 Кеширование эмбеддингов включено (размер: 1000)
```

### Проверка работы

```python
# Проверить, используется ли Hybrid Search
results = storage.search_similar(
    query="тест",
    document_id="session_123",
    top_k=5,
    use_hybrid=True,
)
# В логах должно быть: "🔍 Hybrid Search (alpha=0.5): BM25 + векторный поиск"

# Проверить кеш
print(f"Размер кеша: {len(storage._embedding_cache)}")
```

## Миграция существующего кода

Существующий код продолжит работать без изменений. Все новые возможности опциональны:

```python
# Старый код - работает как раньше
results = storage.search_similar(
    query="тест",
    document_id="session_123",
    top_k=10,
)

# Новый код - использует Hybrid Search автоматически
# (если WEAVIATE_USE_HYBRID_SEARCH=true)
results = storage.search_similar(
    query="тест",
    document_id="session_123",
    top_k=10,
)
```

## Troubleshooting

### Hybrid Search не работает

**Проблема:** Ошибка при использовании Hybrid Search

**Решение:**
1. Проверить версию Weaviate (нужна 1.24+)
2. Убедиться, что коллекция поддерживает BM25 (AutoSchema создает автоматически)
3. Проверить логи на наличие ошибок

### Reranking медленный

**Проблема:** Reranking добавляет слишком много времени

**Решение:**
1. Уменьшить `rerank_limit` (например, с 50 до 20)
2. Использовать reranking только для критических запросов
3. Рассмотреть использование более легковесной модели

### Кеш занимает много памяти

**Проблема:** Кеш эмбеддингов занимает много памяти

**Решение:**
1. Уменьшить `WEAVIATE_EMBEDDING_CACHE_SIZE`
2. Отключить кеширование: `WEAVIATE_EMBEDDING_CACHE_SIZE=0`
