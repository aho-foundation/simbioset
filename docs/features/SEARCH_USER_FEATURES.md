# Пользовательские функции поиска

## Обзор

Поиск в симбиосети теперь использует **zero-config подход** - все технические параметры работают оптимально по умолчанию, а пользователи видят только понятные функции.

## Пользовательские функции

### 1. Фильтры по типу контента

**Вместо технических тегов** - понятные категории:

- **Все** - показать все результаты
- **⚠️ Риски** - найденные проблемы экосистем
- **🔴 Уязвимости** - возможные риски
- **✅ Решения** - предлагаемые решения

**Как работает:**
- Автоматически маппится на теги: `ecosystem_risk`, `ecosystem_vulnerability`, `suggested_ecosystem_solution`
- Пользователь не видит технические детали

### 2. Фильтры по времени

**Быстрые фильтры:**
- **Все время** - без ограничений
- **Сегодня** - только за сегодня
- **Неделя** - за последние 7 дней
- **Месяц** - за последние 30 дней

**Как работает:**
- Автоматически вычисляет timestamp диапазоны
- Фильтрует на уровне Weaviate для производительности

### 3. Фильтр по локации

**Поиск по географическому расположению:**
- Ввод локации: "Москва", "тайга", "Сибирь"
- Автоматический поиск по полю `location`

### 4. Сортировка результатов

**Два режима:**
- **По релевантности** - лучшие совпадения сначала (по умолчанию)
- **По времени** - новые записи сначала

## Zero-Config: Технические параметры

Все технические параметры **скрыты от пользователя** и работают оптимально:

```typescript
// Автоматически применяется:
{
  use_hybrid: true,        // Hybrid Search всегда включен
  hybrid_alpha: 0.5,      // Оптимальный баланс BM25/векторный
  use_reranking: false,    // Выключен для скорости
}
```

**Пользователь не видит:**
- ❌ Hybrid Search Alpha (технический параметр)
- ❌ Cross-Encoder Reranking (технический параметр)
- ❌ Технические теги (ecosystem_risk, ecosystem_vulnerability)

**Пользователь видит:**
- ✅ Тип контента (риски, уязвимости, решения)
- ✅ Временные фильтры (сегодня, неделя, месяц)
- ✅ Локация
- ✅ Сортировка

## Визуальные индикаторы

### Цветные бейджи для типов контента

- **⚠️ Риски** - желтый фон (`#fef3c7`)
- **🔴 Уязвимости** - красный фон (`#fee2e2`)
- **✅ Решения** - зеленый фон (`#d1fae5`)

### Активные фильтры

Показываются в результатах поиска:
- Примененные фильтры по типу контента
- Выбранный временной период
- Указанная локация

## Примеры использования

### Пример 1: Поиск рисков за неделю

```tsx
// Пользователь выбирает:
// - Тип: "⚠️ Риски"
// - Период: "Неделя"

// Автоматически применяется:
{
  tags: ['ecosystem_risk'],
  timestamp_from: weekAgo,
  timestamp_to: now,
  use_hybrid: true,  // Автоматически
  hybrid_alpha: 0.5,  // Автоматически
}
```

### Пример 2: Поиск решений в Москве

```tsx
// Пользователь выбирает:
// - Тип: "✅ Решения"
// - Локация: "Москва"

// Автоматически применяется:
{
  tags: ['suggested_ecosystem_solution', 'ecosystem_solution'],
  location: 'Москва',
  use_hybrid: true,  // Автоматически
}
```

### Пример 3: Новые записи за сегодня

```tsx
// Пользователь выбирает:
// - Период: "Сегодня"
// - Сортировка: "По времени"

// Автоматически применяется:
{
  timestamp_from: todayStart,
  timestamp_to: now,
  // Сортировка на фронтенде
}
```

## Преимущества подхода

### 1. Понятность

- Пользователи видят понятные категории, а не технические теги
- Нет необходимости знать про Hybrid Search, BM25, reranking

### 2. Производительность

- Все технические параметры оптимальны по умолчанию
- Hybrid Search включен автоматически для лучшей точности
- Reranking выключен для скорости (можно включить для критических запросов)

### 3. Гибкость

- Легко добавить новые типы контента
- Легко добавить новые временные фильтры
- Технические параметры можно изменить без изменения UI

## Расширение функциональности

### Добавление нового типа контента

```typescript
// 1. Добавить в тип
type ContentType = 'all' | 'risks' | 'vulnerabilities' | 'solutions' | 'new_type'

// 2. Добавить маппинг
const contentTypeToTags = (type: ContentType): string[] | undefined => {
  switch (type) {
    case 'new_type':
      return ['new_tag']
    // ...
  }
}

// 3. Добавить кнопку в UI
<button onClick={() => setContentType('new_type')}>
  🆕 Новый тип
</button>
```

### Добавление нового временного фильтра

```typescript
// 1. Добавить в тип
type TimeFilter = 'all' | 'today' | 'week' | 'month' | 'year'

// 2. Добавить вычисление
const getTimeRange = (filter: TimeFilter) => {
  switch (filter) {
    case 'year':
      const yearAgo = now - 365 * 24 * 60 * 60
      return { from: yearAgo, to: now }
    // ...
  }
}
```

## Заключение

**Zero-config подход** означает:
- ✅ Все технические параметры оптимальны по умолчанию
- ✅ Пользователи видят только понятные функции
- ✅ Не нужно настраивать Hybrid Search, reranking, alpha
- ✅ Просто выбираешь тип контента, время, локацию - и ищешь!
