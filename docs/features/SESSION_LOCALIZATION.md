# Локализация экосистем в сессиях чата

## Обзор

**Локализация экосистем** - это механизм, который привязывает определенную географическую локацию и связанные с ней экосистемы к сессии чата. Это позволяет LLM давать более релевантные и точные ответы, учитывая локальные условия и особенности экосистем.

## Как это работает

### 1. Привязка к сессии

Каждая сессия чата может иметь **сохраненную локализацию**, которая включает:
- **Географическую локацию** (город, регион, координаты)
- **Локальные экосистемы** (лес, река, городской парк и т.д.)
- **Координаты** (широта, долгота)
- **Источник локализации** (автоматическое определение или ручной ввод пользователя)

### 2. Автоматическая локализация

При первом сообщении в новой сессии система:
1. Анализирует текст сообщения на предмет упоминаний локаций
2. Автоматически сохраняет найденную локализацию в сессии
3. Применяет эту локализацию ко всем последующим сообщениям в сессии

### 3. Ручная локализация

Пользователь может явно указать свою локацию:
1. Нажать иконку 📍 в панели действий диалога
2. Предоставить доступ к геолокации
3. Система определит координаты и ближайшую локацию
4. Локализация сохранится в сессии и будет применяться ко всем сообщениям

## Преимущества

### 1. Контекстуальность
- LLM знает о локальных условиях (климат, сезон, тип экосистемы)
- Ответы учитывают региональные особенности

### 2. Стабильность
- Локализация сохраняется между сообщениями
- Не нужно повторять локацию в каждом сообщении

### 3. Персонализация
- Система адаптируется к географическому контексту пользователя
- Более точные рекомендации по локальным экосистемам

## API endpoints

### POST /api/chat/localize

Локализует экосистему для сессии на основе координат пользователя.

**Запрос:**
```json
{
  "sessionId": "session-uuid",
  "latitude": 55.7558,
  "longitude": 37.6176,
  "conversationText": "Текст текущего диалога для анализа"
}
```

**Ответ:**
```json
{
  "success": true,
  "location": {
    "display_name": "Москва, Россия",
    "latitude": 55.7558,
    "longitude": 37.6176
  },
  "ecosystems": [
    {"name": "городской лес", "scale": "habitat"},
    {"name": "река", "scale": "habitat"}
  ],
  "description": "Экосистема диалога локализована в Москве..."
}
```

### GET /api/chat/localize/{session_id}

Получает текущую локализацию сессии.

**Ответ:**
```json
{
  "success": true,
  "session_id": "session-uuid",
  "location_data": {
    "location": "Москва",
    "ecosystems": [...],
    "coordinates": {...},
    "source": "user_provided"
  },
  "has_localization": true
}
```

### DELETE /api/chat/localize/{session_id}

Сбрасывает локализацию сессии.

**Ответ:**
```json
{
  "success": true,
  "message": "Локализация сессии сброшена..."
}
```

## Формат контекста

Все контексты теперь используют **структурированный формат в стиле Weaviate метрик**:

```
=== CATEGORY NAME ===
🏷️ Entity_N: Название сущности
   ├── Основные свойства | Тип: тип
   ├── Статус: active | Доверие: 0.85
   ├── Описание: текст описания
   │   продолжение описания...
   └── Метаданные: ключ1=значение1, ключ2=значение2

=== SUMMARY ===
📊 Сводная статистика
📊 Статус: active | Тип: категория
```

## Интерфейс пользователя

### Индикатор локализации
В панели действий диалога отображается текущая локализация:
- 📍 Москва - если локализация установлена
- Кнопка сброса (🗑️) - для удаления текущей локализации

### Иконка отправки локации
- 📍 в каждом сообщении и в панели действий
- При нажатии запрашивает доступ к геолокации
- Автоматически определяет ближайшую локацию

## Примеры использования

### Пример 1: Автоматическая локализация
```
Пользователь: "Расскажи про дубы в моем лесу"
Система: Автоматически определяет "лес" как экосистему
LLM: Отвечает с учетом лесной экосистемы, упоминая типичные виды дубов для региона
```

### Пример 2: Ручная локализация
```
Пользователь: Нажимает 📍, предоставляет геолокацию
Система: Определяет "Московская область"
Пользователь: "Какие растения здесь растут весной?"
LLM: Отвечает с учетом московского региона, упоминая местные виды
```

### Пример 3: Сброс локализации
```
Пользователь: Нажимает 🗑️ для сброса локализации
Система: Убирает привязку к конкретной локации
LLM: Отвечает общими рекомендациями без региональной специфики
```

## Техническая реализация

### Сохранение в сессии
```python
# api/chat/service.py
def update_session_location(self, session_id: str, location_data: dict) -> ChatSession:
    session = self._sessions.get(session_id)
    if session:
        session.location = location_data
        session.updated_at = int(time.time() * 1000)
    return session
```

### Использование в контексте
```python
# api/chat/routes.py
session_location_data = chat_session.location if chat_session and chat_session.location else None

if session_location_data:
    location = session_location_data.get("location")
    ecosystems = session_location_data.get("ecosystems", [])
else:
    # Автоматическое определение из сообщения
    ecosystem_data = await extract_ecosystem_and_location(message_data.message)
    location = ecosystem_data.get("location")
    ecosystems = ecosystem_data.get("ecosystems", [])
```

### Модель данных
```python
# api/chat/models.py
class ChatSession(BaseModel):
    # ... другие поля ...
    location: Optional[dict] = Field(None, description="Location and ecosystem data")
```

## Расширение функциональности

### Добавление новых источников локализации
- Интеграция с картами (Yandex Maps, Google Maps)
- Определение по IP адресу
- Социальные сети и профиль пользователя

### Улучшение точности
- Машинное обучение для лучшего определения локаций из текста
- Кластеризация пользователей по регионам
- Исторические данные о локациях

### Интеграция с экосистемами
- Базы данных местных экосистем
- Сезонные изменения в экосистемах
- Влияние климата на рекомендации

## Заключение

Локализация экосистем в сессиях чата позволяет создавать более персонализированные и контекстуально релевантные диалоги. Система автоматически адаптируется к географическому положению пользователя и сохраняет эту информацию для поддержания непрерывности разговора.